spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: cuotas-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/ms_cuotas
    username: postgres
    password: hola123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update